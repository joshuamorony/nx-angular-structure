import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nx-angular-structure',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
